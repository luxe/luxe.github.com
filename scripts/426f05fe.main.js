var NUMBER_OF_SECONDS_TO_WAIT_BETWEEN_UPDATES=5,ATOM_FEED="https://github.com/luxe.atom",API_URL="https://api.github.com/users/luxe/events",update_time_through_atom_feed=function(){$.get(ATOM_FEED,function(a){$(a).find("entry").eq(0).each(function(){var a=$(this).find("published").text(),b=moment(a).fromNow();$("h1").html(b)})})},update_time_through_api_feed=function(){$.getJSON(API_URL,function(a){$(a).eq(0).each(function(){var a=$(this)[0].created_at,b=moment(a).fromNow();$("h1").html(b)})})};$(function(){update_time_through_api_feed(),setInterval(update_time_through_api_feed,1e3*NUMBER_OF_SECONDS_TO_WAIT_BETWEEN_UPDATES)});